@model List<Cliente>
@{
}

@*<a asp-action="Guardar" asp-controller="Cliente" class="btn btn-primary">Crear Cliente</a>
@foreach (var item in Model)
{

    <p>@item.id_cliente</p>
    <p>@item.clie_nombre</p> 
   <p>@item.clie_apellido</p>
   <p> @item.clie_dni</p>
   <p> @item.clie_cuit</p>
   <p> @item.clie_razon_social</p>

    <a asp-action="Editar" asp-controller="Cliente" class="btn btn-primary" asp-route-id="@item.id_cliente">Editar Cliente</a>
    <br>
    <a asp-action="Eliminar" asp-controller="Cliente" class="btn btn-danger" asp-route-id="@item.id_cliente">Eliminar Cliente</a>
}
*@




    <body>
    <div class="container">
		<div class="table-responsive">
			<div class="table-wrapper">
				<div class="table-title">
					<div class="row">
						<div class="col-xs-6">
							<h2>Manejar <b>Clientes</b></h2>
						</div>
						<div class="col-xs-6">
							<a asp-action="Guardar" asp-controller="Cliente" class="btn btn-success"><i class="material-icons">&#xE147;</i> <span>Crear Cliente</span></a>
									
						</div>
					</div>
				</div>
				<table class="table table-striped table-hover">
					<thead>
						<tr>
							
							<th>Id Cliente</th>
							<th>Nombre</th>
							<th>Apellido</th>
							<th>Dni</th>
							<th>Cuit</th>
							<th>Razón Social</th>
							<th>Acción</th>
						</tr>
					</thead>

						@foreach (var item in Model)
							{
					<tbody>
						<tr>
							

							@*@foreach (var item in Model)
							{*@
								<th>@item.id_cliente</th>
								<th>@item.clie_nombre</th>
								<th>@item.clie_apellido</th>
								<th>@item.clie_dni</th>
								<th>@item.clie_cuit</th>
								<th>@item.clie_razon_social</th>
							

							<td>
								<a asp-action="Editar" asp-controller="Cliente" class="edit" asp-route-id="@item.id_cliente"><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>
								@*<a href="#editClienteModal" class="edit" data-toggle="modal"><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>*@
								@*<a href="#deleteClienteModal" class="delete" data-toggle="modal"><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a>*@
								<a asp-action="Eliminar" asp-controller="Cliente" class="bdanger" asp-route-id="@item.id_cliente"><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a>
							</td>
							
						</tr>
					</tbody>
					}
				</table>
	
			</div>
		</div>        
    </div>
	</body>