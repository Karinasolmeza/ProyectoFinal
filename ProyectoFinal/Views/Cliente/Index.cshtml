@model List<Cliente>
@{

	 ViewData["Title"] = "Clientes";
     Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<a asp-action="Guardar" asp-controller="Cliente" class="btn btn-primary">Crear Cliente</a>
@foreach (var item in Model)
{

    <p>@item.id_cliente</p>
    <p>@item.clie_nombre</p> 
   <p>@item.clie_apellido</p>
   <p> @item.clie_dni</p>
   <p> @item.clie_cuit</p>
   <p> @item.clie_razon_social</p>

    <a asp-action="Editar" asp-controller="Cliente" class="btn btn-primary" asp-route-id="@item.id_cliente">Editar Cliente</a>
    <br>
    <a asp-action="Eliminar" asp-controller="Cliente" class="btn btn-danger" asp-route-id="@item.id_cliente">Eliminar Cliente</a>
}
*@


<!DOCTYPE html>

<html>
<head>
	<meta name="viewport" content="width=device-width" />
	<title>Index</title>
	<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/css/toastr.min.css" />
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.css"/>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>

	<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css"></script>


</head>


<body>
    <div class="container">
		<div class="table-responsive">
			<div class="table-wrapper">
				<div class="table-title">
					<div class="row">
						<div class="col-xs-6">
							<h2>Manejar <b>Clientes</b></h2>
						</div>
						<div class="col-xs-6">
							<a asp-action="GuardarCliente" asp-controller="Cliente" class="btn btn-success"><i class="material-icons">&#xE147;</i> <span>Crear Cliente</span></a>
									
						</div>
					</div>
				</div>
				<table class="table table-striped table-hover">
					<thead>
						<tr>
							
							<th>Id Cliente</th>
							<th>Nombre</th>
							<th>Apellido</th>
							<th>Dni</th>
							<th>Cuit</th>
							<th>Razón Social</th>
							<th>Tipo de Cliente</th>

							<th>Id Usuario</th>

							<th>Acción</th>
						</tr>
					</thead>

						@foreach (var item in Model)
							{
					<tbody>
						<tr>
							
								<th>@item.id_cliente</th>
								<th>@item.clie_nombre</th>
								<th>@item.clie_apellido</th>
								<th>@item.clie_dni</th>
								<th>@item.clie_cuit</th>
								<th>@item.clie_razon_social</th>
								<th>@item.clie_tipo</th>

								<th>@item.clie_id_usuario</th>

							
							

							<td>
								<a asp-action="EditarCliente" asp-controller="Cliente" class="edit" asp-route-id="@item.id_cliente"><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>
							
								<a asp-action="EliminarCliente" asp-controller="Cliente" class="bdanger" asp-route-id="@item.id_cliente"><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a>
							</td>
							
						</tr>
					</tbody>
					}


				</table>
			</div>
		</div>        
    </div>

	<script>
		$(function () {
			var mensaje = '@TempData["Mensaje"]';
			if (mensaje) {
				toastr.success(mensaje);
			}
		});

		$(function () {
			var mensajeError = '@TempData["MensajeError"]';
			if (mensajeError) {
				toastr.warning(mensajeError);
			}
		});

		$(function () {
			var mensajeInfo = '@TempData["MensajeInfo"]';
			if (mensajeInfo) {
				toastr.info(mensajeInfo);
			}
		});
	</script>
	</body>
	
</html>